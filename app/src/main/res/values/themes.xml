<resources>
  <!-- Base application theme. -->
  <style name="Base.Theme.MultiImageView" parent="Theme.Material3.DayNight.NoActionBar">
    <!-- Customize your light theme here. -->
    <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    <item name="materialButtonStyle">@style/MyButtonStyle</item>
  </style>

  <style name="Theme.MultiImageView" parent="Base.Theme.MultiImageView" />

  <!-- 定义自定义按钮样式 -->
  <style name="MyButtonStyle" parent="Widget.Material3.Button">
    <item name="android:paddingTop">3dp</item>
    <item name="android:paddingBottom">3dp</item>
    <item name="cornerRadius">10dp</item>
    <item name="backgroundTint">@color/color_select_button</item>
  </style>
</resources>